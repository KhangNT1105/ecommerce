import { storiesOf } from '@storybook/react';
import React from 'react';
import UnderConstruction from './UnderConstruction';

storiesOf('UnderConstruction', module).add('default', () => <UnderConstruction />);
